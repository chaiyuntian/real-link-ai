{
  "name": "real-link-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "sync:r2": "bash scripts/sync_r2_assets.sh",
    "deploy:cloudflare": "npm run build && npm run sync:r2 && npx wrangler deploy",
    "dev:site": "python3 -m http.server 8000"
  }
}
